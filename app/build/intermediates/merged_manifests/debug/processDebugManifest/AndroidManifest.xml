<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pscholer.autoplayer.debug"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="29"
        android:targetSdkVersion="35" />

    <!-- ── Storage ─────────────────────────────────────────────────────── -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ── Network ─────────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ── Playback service ─────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        ACCESS_SURFACE: grants the app access to the car display's Surface object.
        Without this the SurfaceCallback fires but getSurface() returns null.
        This is the single most critical permission for video-on-Android-Auto.
    -->
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />

    <!-- Declares a default attribution tag to avoid AppOps warnings during media playback -->
    <attribution
        android:label="@string/app_name"
        android:tag="media_playback" />

    <queries>
        <provider
            android:name="androidx.car.app.connection.provider"
            android:authorities="androidx.car.app.connection" />
    </queries>

    <permission
        android:name="com.pscholer.autoplayer.debug.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.pscholer.autoplayer.debug.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:name="com.pscholer.autoplayer.AutoPlayerApp"
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoPlayer" >

        <!-- Phone-side configuration UI (launcher entry point) -->
        <activity
            android:name="com.pscholer.autoplayer.SettingsActivity"
            android:exported="true"
            android:label="Auto Player" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            CarAppService — the Android Auto entry point.

            Category NAVIGATION is used (not MEDIA) because:
            1. Navigation apps receive full-screen SurfaceContainer access.
            2. MEDIA apps are sandboxed into the AA media browser UI.
            3. Fermata Auto and similar sideloaded video players use this trick.

            For a Play Store build you would need to switch to MEDIA and accept
            the standard media-browser restrictions.
        -->
        <service
            android:name="com.pscholer.autoplayer.service.AutoMediaService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback" >
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />

                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>

        <!-- Tells the AA host which car features this app uses -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!--
            Minimum Car App API level.
            Level 2 introduced SurfaceCallback / SurfaceContainer.
            Required for video surface access.
        -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="2" />

        <service
            android:name="androidx.car.app.CarAppMetadataHolderService"
            android:enabled="false"
            android:exported="false"
            android:process="" >
            <meta-data
                android:name="androidx.car.app.CarAppMetadataHolderService.CAR_HARDWARE_MANAGER"
                android:value="androidx.car.app.hardware.ProjectedCarHardwareManager" />
        </service>

        <activity
            android:name="androidx.car.app.CarAppPermissionActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <receiver
            android:name="androidx.car.app.notification.CarAppNotificationBroadcastReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.pscholer.autoplayer.debug.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>